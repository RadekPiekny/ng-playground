import { PathLightningDirective } from './path-lightning.directive';

describe('PathLightningDirective', () => {
  it('should create an instance', () => {
    const directive = new PathLightningDirective();
    expect(directive).toBeTruthy();
  });
});
